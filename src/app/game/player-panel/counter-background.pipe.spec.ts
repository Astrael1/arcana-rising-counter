import { CounterBackgroundPipe } from './counter-background.pipe';

describe('CounterBackgroundPipe', () => {
  it('create an instance', () => {
    const pipe = new CounterBackgroundPipe();
    expect(pipe).toBeTruthy();
  });
});
